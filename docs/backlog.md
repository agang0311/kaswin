# Backlog

- Publish the single HTML release and standalone indexer package after the real-network gate passes.
- Re-evaluate randomness only if an independent chain-native beacon becomes available; current selected-chain PoW randomness retains the documented target-miner withholding boundary.
