# 最小必要验证

不设覆盖率和测试数量目标。当前只有设计文档，没有代码测试套件。以下是未来验证义务，不是已执行结果。

| 风险 | 不验证的后果 | 最小证据方向 |
|---|---|---|
| 可选择随机结果/看结果取消 | 公平性失效 | 攻击分析 + 固定证据选择/状态约束 VM 负例；网络假设独立验证 |
| 伪造票权、改收款人、侵蚀储备 | 资金或票权被盗 | 小型真实 VM 正负例与金额守恒检查 |
| 合法状态无法终结 | 正常用户资金锁死 | 空轮/未满/满额路径及最坏合法分布资源验证 |
| 无许可机制仅在 UI 成立 | 创建者消失或换客户端即失效 | 直接交易调用与非创建者恢复推进 |
| 链上记录显示错误 | 不透明，可能对失效状态签名 | 历史重建、当前 UTXO/acceptance 对账和撤销场景 |
| 钱包签名与实际支付不符 | 用户非预期授权/跨网络损失 | 最小交易绑定及网络/授权边界检查 |
| 浏览器无法完成交付 | 项目不完整 | 单文件产物与一个完整浏览器流程；后续授权 TN10 验证真实接受/relay |

相同风险在同层不重复堆用例。未来类型、未支持规模、旧协议兼容、非关键视觉快照、只为报告而存在的证据流水线不引入。当前文档检查仅核对本地引用与关键设计一致性，不为文档创建测试框架。

## 固定源码核验与交付要求更新（2026-09-05）

核对 rusty-kaspa HEAD 为 cfafeb4c093fa37a303f1b9f19c58f986b870ce3，阅读 OpTxInputDaaScore、OpChainblockSeqCommit、SeqCommitAccessor 与 transaction finalization 实现。发现链锚执行深度窗口与 locktime/sequence 边界，记录 feasibility-01.md。只有 S 证据，不代表 VM 或真实网络可用性。

明确最终单 HTML、保留中间产物、主助手负责重大决策；未派发编码 agent，未实现代码。一次性本地引用检查 7 个引用、0 缺失；没有为纯文档修改新增代码测试。

## 历史证明恢复候选（2026-09-05）

核对 KIPs HEAD e4ae2332117b5cb68bd6188e065ef885b6d17939；阅读 accessor 全文、proving-spec Reserved 全文、KIP-21 承诺递推相关章节与节点 seqcommit threshold 集成测试源码。没有运行该集成测试。新增 randomness-recovery.md，比较分步历史验证和压缩证明，区别不可变开奖目标与近期验证锚。证据层仍为 S/设计推导，无合约实现、证明生成、VM 或网络验证。本地引用检查 9 个、0 缺失，无新增代码测试、无删除中间材料。

## 工作台父级验收（2026-09-05）

指定 cpa/gemini-3.8-flash-high worker 完成离线工作台，父级审查修复 DAA/blue score 混称、DAA Number 隐式转换、金额格式化 Number 接受和总额非法参数。保留旧测试文件于 research/retained-tests，合并交付测试：去掉文件最小体积与固定风险文案断言，保留自包含/未部署声明/确定构建/历史保留；保留测试改为隔离临时目录，不污染真实 dist。

实际 `npm test` 27/27（含嵌套项），`npm run build` 通过。Chromium headless 直接 file:// 打开产物并 dump DOM，默认预览显示 1,000 KAS = 100,000,000,000 sompi；这是启动/渲染检查，不代表完整浏览器导入导出或视觉无障碍审计。DOM、stderr 与测试日志留在 research/。HTML 为 56,368 bytes，SHA256 b1a9292bea5bf53e0440e47ba5eb1556b6ace2bfde9c3fc9e8cfcab3dd5a8899。

该产物仅离线草案工作台，无合约/SDK/钱包/网络交易；最终链上产品验收尚未通过。没有提交或推送。

## R3 成本审查（2026-09-05）

核对 KIP-21 §5.3–5.4、节点 seq-commit hashing/verify 与 RPC service 的 lane proof 返回字段；固定 SDK d.ts 未发现 getSeqCommitLaneProof 声明。记录 recovery-cost-decision.md：shortcut 不是常数成本证明，R1/R2 降为备用，不部署重证书。仅 S 与设计推导，无费用实测、无 TKAS 支出。文档本地引用 11 个、0 缺失；未为文档添加测试。

## 实际执行

2026-09-05 UTC：创建本地设计骨架与 GitHub 空仓库。无依赖安装、无实现、无私钥操作、无签名/广播、无节点启动、无 git commit/push。一次性本地文件链接检查：4 个引用，0 个缺失；此检查不验证外链、Markdown 全语义或协议安全性。研究底座按其工作约定运行 21/21 基础测试、链接检查和固定 SDK 离线加载；这些并非新 Kaswin 的测试或安全证据。未运行合约编译/VM 或网络验证。
