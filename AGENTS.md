# Kaswin 开发约定

默认中文，先读 README.md 和 docs/ 下的设计。用户授权持续设计，不再逐项询问；设计和重大决策由主助手负责、记录理由和风险，不以未经论证的假设伪装完成。新增真实密钥/网络交易或 commit/push 等受限操作仍遵循授权边界。

最终交付固定为单个自包含 index.html；源码、设计、构建输入/产物和必要验证材料均保留，不删除中间产物，不执行破坏性 clean。不能因最终单文件而把源码也写成单体。

实现任务可交给 gemini-3.8-flash-high 驱动的 subagent；派发前发现可执行 agent 与模型，确认确为指定模型，不擅自换模型。主助手制定契约和必要验证、审查实现并负责重大决策；此授权不强制现在派发。

## Git 交付约定

用户已明确长期授权：以后任务完成后默认创建独立 commit 并 push 到 GitHub，核对本地 HEAD 与目标远端分支 SHA 一致后返回完整 SHA。当前目标分支为 `audit/state-deposit-v1`；不自动合并 main，不 amend/rebase/squash/force push。若用户明确要求提交未完成工作，按 BLOCKED 检查点如实提交，不把提交成功称为功能门禁通过。此授权不扩大钱包、签名、节点或广播权限；凭证不得进入提交、日志或 remote URL。

## 边界

- Kaspa 非 EVM。区块包含不等于 transaction acceptance。
- 任何人可创建；公平透明、无特权操控、链上裁决、所有业务动作可查、单网页是硬要求。
- 固定上游 commit/tag，记录获取日期与验证层。外部源码放忽略目录 references/，不安装猜测的同名 SDK。
- 单写入者；金额使用 bigint/十进制字符串，不用 Number 或隐式 JSON 转换。
- 用户于 2026-09-05 明确授权创建专用 TN10 钱包、向 https://faucet-tn10.kaspanet.io/ 申请 TKAS，并为 TN10 实际测试签名/广播。权限仅限该测试钱包与经核验的 TN10；禁止主网、其他真实钱包和凭证泄露。钱包仅存被忽略 wallets/（0700 目录、0600 文件），不得嵌入最终 HTML。节点启动、git commit/push 仍须另行明确授权。允许的 GitHub 仓库管理不等于允许自动提交代码。
- 凭证不得进入文件、日志、remote URL 或聊天。不要搬入旧协议兼容层和整个旧测试工程。

## 最小必要测试

每项测试必须能说明：保护的核心价值/完整性、具体失败场景、遗漏后果、最低足够验证层，以及是否重复。不追逐覆盖率、用例数量或证据报告规模。

不能论证必要的测试不新增；现有测试先核查，冗余合并、无必要性移除。未理解不等于无必要。不以 JS 模拟替代 VM，不以 VM 替代真实节点 acceptance/relay；跨层重复须有不可替代理由。

当前没有实现代码，不创建空测试套件、CI、包依赖或测试管理框架。更改设计检查本地链接和设计一致性，在 docs/validation.md 记录实际范围；未来按协议风险建立最小正负例。
